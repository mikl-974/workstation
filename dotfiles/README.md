# dotfiles/

Bibliotheque des fichiers applicatifs consommes par Home Manager.

## Consommation actuelle

Les dotfiles de ce dossier sont actives principalement par :

- `home/roles/desktop-hyprland.nix`
- `home/targets/ms-s1-max.nix`

Le point d'entree utilisateur reste `home/`, pas `targets/`.
