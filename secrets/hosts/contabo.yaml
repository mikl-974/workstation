# Encrypted host secrets for `contabo`.
#
# Both placeholders below are still encrypted-stub values. Materialize them
# with the real Contabo SSH host key and Tailscale auth key before the first
# real install; the plumbing here only provides the source-controlled slot.
contabo:
  ssh_host_key: ENC[AES256_GCM,data:REPLACE_ME,iv:REPLACE_ME,tag:REPLACE_ME,type:str]
  tailscale_auth_key: ENC[AES256_GCM,data:REPLACE_ME,iv:REPLACE_ME,tag:REPLACE_ME,type:str]
