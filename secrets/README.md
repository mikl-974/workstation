# secrets/

Secrets chiffres avec SOPS.

## Arborescence active

- `secrets/common.yaml`
- `secrets/hosts/ms-s1-max.yaml`
- `secrets/hosts/contabo.yaml`
- `secrets/stacks/*.yaml`
- `secrets/cloud/*.yaml`

## Regle

- modifier via `sops`
- ne documenter ici que les secrets encore consommes
- supprimer les secrets d'anciens hosts quand ils sortent du perimetre
