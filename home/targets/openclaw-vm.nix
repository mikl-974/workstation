#
# `openclaw-vm` is a service-oriented host.
# It intentionally keeps an empty Home Manager binding for now:
# - the flake model stays consistent (`home/targets/<host>.nix` exists)
# - no desktop/user-space composition is forced onto this VM
{}
