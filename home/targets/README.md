# home/targets/

Binding final Home Manager par machine.

## Fichiers actifs

- `ms-s1-max.nix`
- `contabo.nix`

## Regle

Tout host NixOS expose explicitement `home/targets/<host>.nix`.

### `ms-s1-max.nix`

- user `mfo`
- desktop Hyprland
- Noctalia

### `contabo.nix`

- vide
- intentionnel pour un serveur headless
