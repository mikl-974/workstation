# foot terminal configuration
#
# Files in this directory are managed by Home Manager (home/roles/*.nix, home/users/*.nix, or home/targets/*.nix).
# They are symlinked to ~/.config/foot/ on the active machine.
#
# Active contents:
#   foot.ini             — main foot terminal config (font, colors, padding)
#
# If Noctalia gains dedicated terminal assets later, keep them in
# dotfiles/noctalia/ and reference them explicitly from here.
