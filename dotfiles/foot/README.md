# foot terminal configuration
#
# Files in this directory are managed by Home Manager (home/default.nix).
# They are symlinked to ~/.config/foot/ on the active machine.
#
# Typical contents:
#   foot.ini             — main foot terminal config (font, colors, padding)
#
# Noctalia color overrides belong in dotfiles/noctalia/ and are sourced
# or included from foot.ini to keep the color scheme centralized.
